# 番茄钟界面设计评审与毛玻璃优化方案

评审对象：`src/index.html` 中「番茄钟」Tab（panel-pomo）
评审方法：Impeccable 设计评审流程（反模式检测 / Nielsen 启发式评分 / 优先级问题 / 角色走查）
日期：2026-08-25

---

## 一、现状快照

- 结构：两张白色实底卡片
  - 计时卡：模式分段控件（专注/短休/长休）+ 208px SVG 进度环 + 3 按钮（开始/重置/跳过）+ 轮次点 + 4 个时长数字输入框常驻
  - 统计卡：stat-strip 四格 + 今日完成记录列表
- 主题：米白底 `#f7f4ec`、纯白卡、近黑主色 `#1a1a1a`、休息琥珀 `#b45309`
- 全页背景为纯色，卡片之间无重叠、无纹理、无纵深
- 现有玻璃用法：顶部导航 `blur(8px)`（滚动时有内容穿过，正确）、hero chips `blur(4px)`（正确）

## 二、反模式判定（AI Slop 检测）

**中等风险。** 「白卡 + 分段控件 + 圆环 + 统计卡」是 AI 生成界面的典型形态——功能齐全、语言正确，但没有记忆点。更需要注意：**直接给全卡片糊 backdrop-filter 恰好是 Impeccable 明确列出的反模式（毛玻璃滥用）**，而且在本页面会失效（见下）。

## 三、核心发现：毛玻璃的物理前提不成立 [P0]

玻璃需要**背后有东西可折射**。`backdrop-filter: blur()` 模糊的是元素背后的内容；当前页面主体是纯色米白背景 + 白色卡片，卡片间零重叠——模糊一片均匀颜色，视觉结果与不透明卡片几乎无差别，只剩下白色半透明带来的对比度损失。

当前设计里唯二「正确」的玻璃是导航条和 hero chips，因为它们背后有真实内容（滚动的列表 / 深色渐变纹理）。

**结论：要毛玻璃成立，必须先制造纵深，再放玻璃。**

## 四、优化方案（三选一或组合）

### 方案 A：深色墨面舞台（推荐）

番茄钟 Tab 的**计时卡整体换成深色墨面**：复用 hero 的黑→暖炭渐变（`#141414 → #3a352a`）与白点纹理；模式分段控件、进度环、次级按钮（重置/跳过）变成玻璃控件浮在墨面上；主按钮与时间数字保持实底。

- 浅色玻璃压深色纹理 = 折射效果最真实、实现最廉价
- 功能语义成立：专注 = 进入沉浸暗环境，与其他 Tab 的明亮工作态形成节奏对比
- 统计卡/记录卡保持米白实底，只让"舞台"暗下来，避免全页变浑

玻璃规格（设计 token，非代码）：

| Token | 值 |
|---|---|
| 玻璃面填充 | 白 8–14% 不透明度 |
| 模糊半径 | 16–24px（不超过 24） |
| 玻璃描边 | 1px 白 25% |
| 进度轨道 | 白 14% |
| 专注进度弧 | 米白 `#f5f2ea` |
| 休息进度弧 | 琥珀提亮 `#f59e0b`（原 `#b45309` 在深底对比不足） |
| 时间数字 | 纯白实底级对比，不透玻璃 |
| 对比度红线 | 深底正文 ≥4.5:1；玻璃面只承载次级信息 |
| 降级 | `@supports` 不支持 backdrop-filter 时给实底 fallback |
| 性能红线 | 玻璃元素 ≤5 个，避免低端安卓/iOS 省电模式卡顿 |

### 方案 B：全局环境光斑（最小改动，但影响全站）

给 body 叠加多层柔和暖色 radial-gradient 光斑或纸纹，让所有卡片可半透明 + blur。缺点：五个 Tab 的风格全部被拖进玻璃拟态，与现有纯净纸感冲突，卡片可读性下降。**不推荐单用。**

### 方案 C：悬浮玻璃计时器

计时环做成 sticky 悬浮玻璃元素，滚动时覆盖在记录列表上方，列表内容从玻璃下滑过——blur 有真实内容可模糊。适合记录变多的后续场景，但改变交互行为，手机端会压缩列表可视区。**可作为 A 之后的增强。**

**推荐组合：A 为主，C 作为二期增强。**

## 五、顺带发现的其他设计问题

1. **[P1] 4 个数字输入框常驻**——渐进披露不足。90% 的时间用户不改 25/5/15/4，却每次占一整行，还给新用户"我需要先配置"的错误暗示。应收进折叠的「设置」区。
2. **[P1] 模式无色彩语义**——三个模式仅文字区分，只有进度环在休息时换色。分段控件应让短休/长休带琥珀色调，扫一眼即知当前态。
3. **[P2] 完成时刻太安静（峰终定律）**——专注完成只有提示音 + Toast。项目已有目标达成彩带动效，番茄完成应复用：这是整个 Tab 情绪的最高点。
4. **[P2] 跳过无确认**——误触「跳过」直接换段丢进度。至少给 2 秒内可撤销的 Toast。
5. **[P3] 时间字体无性格**——44px 系统默认粗体作为舞台主角偏"默认"。A 方案的黑底白字天然放大数字的锚点感，可顺势用 tabular 显示字体强化。

## 六、Nielsen 启发式评分（30/40 · 良好偏上）

| # | 启发式 | 分 | 关键问题 |
|---|---|---|---|
| 1 | 系统状态可见 | 3 | 环 + 标签页标题同步倒计时，好 |
| 2 | 贴近真实世界 | 4 | 番茄钟/专注/休息术语准确 |
| 3 | 用户控制与自由 | 2 | 跳过无确认、无撤销 |
| 4 | 一致性与标准 | 4 | 分段控件/按钮/卡片与全站语言一致 |
| 5 | 错误预防 | 3 | 输入框有 min/max 边界 |
| 6 | 识别而非回忆 | 3 | 模式与状态均有文字标签 |
| 7 | 灵活与效率 | 2 | 无键盘快捷键；改时长需动 4 个框 |
| 8 | 美学与极简 | 2 | 控件直给堆叠，输入框常驻 |
| 9 | 错误恢复 | 3 | 全局读写失败提示+重试已具备 |
| 10 | 帮助与文档 | 3 | 空状态文案友好 |

## 七、角色走查红旗

- **Jordan（首次使用）**：进 Tab 首屏同时出现模式 + 环 + 3 按钮 + 4 输入框 + 统计 + 记录，元素偏多，不确定先点哪；输入框常驻加重配置焦虑。
- **Alex（高频用户）**：每天多次打开，没有键盘快捷键、没有"一键再来一轮"的极简路径；误触跳过成本高。

## 八、已确认的方向与范围（2026-08-25 用户决策）

- **玻璃方向：方案 C —— 悬浮玻璃计时器**
  - 计时环区域（模式分段控件 + 进度环 + 时间 + 主操作按钮 + 轮次点）整体做成 sticky 悬浮玻璃元素
  - 玻璃规格仍按第四节 token 表执行：白 8–14% + blur 16–24px + 1px 白 25% 描边；blur 的折射内容 = 滚动到其下方的统计卡与记录列表
  - 时间数字与主按钮保持实底对比；深色文字（近黑 `#1a1a1a`）压在浅色玻璃上，对比需 ≥4.5:1
  - 移动端 sticky 需注意：单列布局下悬浮区高度控制在视口 45% 以内，避免压缩列表可视区；桌面端左栏计时器 sticky、右栏列表滚动，天然适配
  - `@supports` 降级：不支持 backdrop-filter 时回退为不透明白卡 + 加重描边
  - 性能红线：整页玻璃元素 ≤3 个（悬浮计时器 + 导航 + 可能的次级件），blur ≤24px

- **执行范围：玻璃 + P1 两项**
  1. 悬浮玻璃计时器（视觉主体）
  2. [P1] 4 个时长输入框收进折叠的「设置」区（默认收起，展开后仍可修改，min/max 不变）
  3. [P1] 模式色彩语义：分段控件「短休息/长休息」态与休息进度弧统一琥珀色调，扫一眼即知当前态

- 暂不做（后续可选）：P2 完成彩带复用、P2 跳过可撤销

## 九、建议执行顺序（按已确认范围）

1. 悬浮玻璃计时器（sticky 玻璃 + 滚动折射）
2. P1 两项：输入框收进设置折叠 + 模式色彩语义
3. 最后走一遍 polish：对齐、间距、对比度终检
