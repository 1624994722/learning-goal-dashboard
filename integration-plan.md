# 整合规划：学习目标管理台 × 暗色电影感落地页

> 版本 v1.0 · 2026-08-26 · 纯规划文档，不含代码
> 目标：把现有学习目标管理台的全部功能融入暗色电影感落地页，形成一个「营销级首屏 + 实用工作台」的完整产品页面

---

## 一、功能梳理

### 1.1 现有功能清单（来自学习目标管理台 src/index.html）

| # | 功能模块 | 内容 | 优先级 | 复杂度 | 备注 |
|---|---------|------|--------|--------|------|
| F1 | 目标管理 | 新建/编辑/删除目标（名称、单位、总量、截止日、配色、障碍预案+对策）、二次确认删除 | P0 | 中 | 核心数据模型，一切的基础 |
| F2 | 今日打卡 | 今日建议量计算（剩余量÷剩余天数）、打卡/补记（最近6天）、分钟数选填、多次打卡 | P0 | 中 | 最高频交互 |
| F3 | 状态智能 | 连续天数（每周1休息日规则）、昨日漏打置顶（黄卡/红卡）、逾期天数、预计完成日（近7天均速推算） | P0 | 高 | 业务逻辑最复杂的部分 |
| F4 | 进度看板 | 进度环、三核心数字、预案展示、最近10条记录（可删）、14天分钟堆叠柱状图、热力图 | P1 | 中 | 视觉密度最高 |
| F5 | 番茄钟 | 专注/短休/长休三模式、SVG 进度环、悬浮玻璃计时器、可调时长、提示音、今日统计 | P1 | 中 | 已含毛玻璃设计，与暗色主题天然契合 |
| F6 | 周报复盘 | 周汇总+环比、历史周查看、四栏（保持/问题/尝试/下周预案）、一键生成周报文本 | P1 | 低 | 纯展示+表单 |
| F7 | 数据层 | localStorage 本地持久化 + 资料库云端双向同步（打卡记录写云端数据表）、失败重试与提示 | P0 | 高 | 云端同步依赖运行环境 |
| F8 | 动效体系 | GSAP 入场/Tab 过渡/按压弹性/数字滚动、Anime.js 进度环、IO 滚动 reveal、降级方案 | P2 | 低 | 已模块化，可整体迁移 |
| F9 | 我的/设置 | 同步状态、手动重同步、清空示例/全部、添加到主屏幕说明 | P2 | 低 | 工具属性 |

### 1.2 落地页现有元素（landing/index.html）

| # | 元素 | 整合中的角色 |
|---|------|------------|
| L1 | 视频 Hero 舞台（CloudFront 循环视频 + fade overlays） | 保留为首屏，作为产品门面 |
| L2 | 品牌 S 形 SVG + 导航 + Get Started pill | 导航重定向到工作台锚点；CTA 成为「进入工作台」入口 |
| L3 | 底部 logoipsum 合作条 | 保留，可下移到页尾 |
| L4 | 像素锁定单位系统（--u/--h） | Hero 区保留；工作台区改用常规流式布局（两套体系共存，见 §2.3） |
| L5 | Manrope 字体 + 暗色 token | 扩展为全站设计系统基础 |

### 1.3 关键冲突预判

- **主题冲突（最大工作项）**：管理台是米白亮色主题，落地页是纯黑暗色。管理台全部 UI 需要暗色化改造，不是简单搬家。
- **布局冲突**：落地页是 `overflow:hidden` 固定视口舞台，管理台是长滚动页面。必须打破单一视口限制。
- **文案冲突**：落地页文案是 "The Next Layer of Intelligence"（AI 基建），管理台是学习工具。需要决定：改文案贴合学习产品，还是保留氛围。

---

## 二、整体架构规划

### 2.1 推荐架构：单页双层结构（Landing Shell + Workspace）

```
┌─────────────────────────────────────┐
│  第 1 屏：Hero 舞台层（100vh 锁定）    │  ← 现有 landing 原样保留
│  视频背景 + 品牌 + 导航 + 标题 + CTA    │
│  CTA「Get Started」→ 平滑滚动到工作台   │
│  Ghost「View Architecture」→ 滚动到看板 │
├─────────────────────────────────────┤
│  第 2 层：工作台区（正常文档流，可滚动）  │
│  ┌─────────────────────────────┐   │
│  │  模块导航（吸顶 Tab 条）       │   │
│  │  今日 │ 番茄钟 │ 看板 │ 周报 │ 我的 │   │
│  ├─────────────────────────────┤   │
│  │  当前激活模块的内容区           │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│  第 3 屏：页尾（logoipsum 条 + 版权）   │
└─────────────────────────────────────┘
```

**为什么选这个而不是多页/SPA 路由**：保持单文件交付物形态；Hero 的"一屏一构图"规格不被破坏；工作台本来就是 Tab 式单页应用，直接作为页面的一段即可。

### 2.2 模块组织

| 区域 | 内容 | 来源 |
|------|------|------|
| Hero 舞台 | 视频、品牌、导航、标题、CTA | L1-L4 原样 |
| 工作台外壳 | 吸顶 Tab 条 + 内容容器 | F2-F6/F9 的现有 Tab 体系平移 |
| 页尾 | logoipsum 四标 + 版权行 | L3 下移 |
| 隐藏层 | 目标编辑弹窗、Toast、彩带 | F1/F8 原逻辑 |

### 2.3 双单位系统共存策略（重要决策）

- **Hero 层**：保留 `--u/--h` 像素锁定系统，不动一个数字——这是规格红线。
- **工作台层**：放弃像素锁定，用常规响应式（rem + clamp + 媒体查询），因为工作台内容高度不确定，无法锁定视口。
- **衔接**：`<html>` 的 `overflow:hidden` 只保留在 Hero 全屏态？——不可行。正确做法：把 `html,body{overflow:hidden}` 改为正常滚动，Hero 舞台改为 `height:100vh` 的普通区块，视频用 `position:absolute` 限制在 Hero 区块内（不再是 fixed 全屏）。这是对落地页唯一的结构性修改。

### 2.4 导航结构

- **桌面端**：Hero 顶部导航（About/Features/FAQ/Contact）改为（工作台/看板/周报/关于）或直接保留英文做锚点跳转；进入工作台后，吸顶 Tab 条接管模块切换。
- **移动端**：burger 菜单项同样改为锚点 + Tab 触发的组合（点菜单项 → 滚动到工作台并激活对应 Tab）。

---

## 三、整合顺序（六个阶段，含依赖关系）

### 阶段 0：基线冻结（0.5 天）
- **目标**：锁定两份现有产物作为合并基线。
- **做什么**：git 提交 landing/index.html；从 src/index.html 中提取三份清单——CSS 变量清单、JS 函数清单（约 60 个）、DOM 结构清单。
- **预期结果**：一份合并工作底稿，明确哪些代码原样搬、哪些要改。
- **依赖**：无。

### 阶段 1：页面骨架重构（1 天）★ 一切的前提
- **目标**：把落地页从「固定视口」改为「可滚动双层结构」。
- **做什么**：
  1. 解除 `html,body` 的 `overflow:hidden`；
  2. Hero 舞台从 `position:fixed` 改为 `height:100vh` 的普通区块，视频/plate 改为区块内绝对定位；
  3. 验证 Hero 在 1487×1058 下像素不偏移（回归检查规格）；
  4. logoipsum 条从 Hero 绝对定位中拆出，移到页尾。
- **预期结果**：落地页视觉零变化，但页面可以向下滚动，为工作台腾出空间。
- **依赖**：阶段 0。**此阶段有风险，必须先做且单独验证**（详见 §5-R1）。

### 阶段 2：暗色设计系统扩展（1 天）
- **目标**：把工作台需要的全部 UI token 暗色化，形成统一设计系统。
- **做什么**：
  1. 在现有 6 个暗色 token 基础上，补充工作台需要的语义 token：卡片底（近黑 #101010）、边框（白 8-12%）、成功绿/逾期红/休息琥珀的暗色变体（降低明度、提高饱和度适配黑底）；
  2. 管理台的米白/纯白/近黑三色映射为暗色三等阶（#050505 舞台 / #101010 卡片 / #1a1a1a 悬浮）；
  3. 番茄钟毛玻璃规格反转：白 16% 玻璃 → 白 6-8% 玻璃 + 白 12% 描边（黑底上玻璃更亮才可见）；
  4. 字体统一 Manrope，工作台字重映射（800→700，暗底上细字更重显）。
- **预期结果**：一份暗色 token 表 + 工作台全部组件的暗色样式表。
- **依赖**：阶段 1（骨架定了才知道工作台住在哪）。可与阶段 3 并行。

### 阶段 3：业务逻辑平移（1.5 天）
- **目标**：管理台的全部 JS 逻辑零改动迁入，数据层先行。
- **做什么**：
  1. 原样搬运纯函数层：日期工具、连续天数、休息日判定、预计完成日推算、周报汇总（这些是框架无关的，一个字不用改）；
  2. 原样搬运状态层：localStorage 读写、数据迁移（含配色迁移表）、云端同步（pushRemote/pullRemote）；
  3. 渲染函数（renderToday/renderBoard/...）DOM id 保持不变，只改它们输出的 class 对应的 CSS；
  4. 番茄钟逻辑整体搬运。
- **预期结果**：在工作台容器内，全部功能可用（此时样式可能是临时的，但功能完整）。
- **依赖**：阶段 1。**刻意与样式解耦：先功能对，再好看。**

### 阶段 4：工作台 UI 暗色化 + 整合导航（1 天）
- **目标**：工作台视觉落地，与 Hero 融为一体。
- **做什么**：
  1. 应用阶段 2 的 token，逐模块过：目标卡、黄/红提示卡、进度环、热力图、柱状图、番茄钟、周报表单；
  2. 吸顶 Tab 条做暗色毛玻璃（复用番茄钟玻璃规格，保持全站玻璃语言统一）；
  3. Hero CTA 接线：「Get Started」→ 滚动到工作台；「View Architecture」→ 滚动到看板 Tab；
  4. 移动端 burger 菜单项接锚点。
- **预期结果**：从 Hero 滚动到工作台有连贯的暗色体验，无"两个网站拼起来"的割裂感。
- **依赖**：阶段 2 + 阶段 3。

### 阶段 5：动效融合（0.5 天）
- **目标**：两套动效体系合并为一个编排。
- **做什么**：
  1. Hero 入场 stagger（rise/riseNav/fade）保留，滚动到工作台时工作台的 IO reveal 接管；
  2. 统一缓动：落地页的 `cubic-bezier(.22,1,.36,1)` 设为全站标准，管理台动效缓动向它对齐；
  3. Tab 切换过渡、按压弹性、数字滚动、进度环 anime 补间原样保留；
  4. 全站 reduced-motion 审计（两套体系各自有降级，合并后统一走一个开关）。
- **预期结果**：滚动—入场—交互的动效节奏全站一致。
- **依赖**：阶段 4。

### 阶段 6：端到端验收（0.5 天）
- **目标**：按两份原始规格 + 功能清单全量回归。
- **验收清单**：Hero 像素回归（1487×1058 截图比对）、5 模块功能全通、暗色对比度 ≥4.5:1、移动端断点、视频加载失败兜底、云端同步读写、reduced-motion。
- **依赖**：阶段 5。

**总工期估算：约 5 天。关键路径：阶段 1 → 阶段 3 → 阶段 4。**

---

## 四、数据与交互设计

### 4.1 数据流（不改动现有设计，仅说明如何嵌入新页面）

```
用户操作（打卡/新建/补记/删记录/存复盘）
        │
        ▼
内存状态 state { goals, logs, notes }
        │
        ├─→ localStorage（即时，离线可用）
        │
        └─→ 云端数据表（pushRemote 即时写回；失败 → Toast 提示 + 可重试）
        ▲
页面加载：先渲染界面 → pullRemote 拉取合并 → 重新渲染
（资料库同步依赖 workbuddy 运行环境；独立部署时自动降级为纯本地，已有此逻辑）
```

**整合要点**：数据层对 Hero 层完全透明——Hero 是静态营销内容，不读业务数据。唯一例外（可选增强）：Hero 的 chips 区域可以显示「进行中 N 个目标」作为产品感点缀，读 state 只读不写。建议放在 P2。

### 4.2 核心用户旅程

1. **新访客**：落地 Hero → 视频 + 标题建立氛围 → 点「Get Started」→ 平滑滚动到工作台「今日」Tab → 看到 3 个示例目标 → 试打卡一次 → Toast 确认。
2. **回访用户**：打开页面 → Hero 快速掠过（或提供跳过）→ 工作台处理今日要事（漏打红/黄卡置顶）→ 番茄钟专注。
3. **复盘时刻**：周末 → 周报 Tab → 看汇总和环比 → 填四栏 → 一键生成文本复制走。

### 4.3 交互衔接细节

| 衔接点 | 设计 |
|--------|------|
| Hero → 工作台滚动 | CTA 点击平滑滚动；Tab 条到达顶部时吸顶（position:sticky） |
| 工作台 → Hero 返回 | Tab 条上保留小品牌标，点击滚回顶部 |
| 菜单（移动端） | 菜单项 = 锚点 + Tab 激活的组合指令 |
| 弹窗/Toast | 层级高于一切（弹窗 z-index 高于吸顶 Tab） |
| 目标达成彩带 | 保留；颜色在暗底上微调亮度 |

---

## 五、风险与注意事项

| # | 风险 | 等级 | 说明与应对 |
|---|------|------|-----------|
| R1 | **Hero 像素回归** | 高 | 阶段 1 把 fixed 舞台改为可滚动区块是唯一的结构性修改，极易破坏 1487×1058 规格。应对：改完立即截图比对原规格；`--u/--h` 单位系统一个字符不动；只动定位上下文（fixed→absolute, overflow 放开） |
| R2 | **暗色化对比度不达标** | 中 | 米白主题的灰阶文字（--ink-2/--ink-3）直接反转会太暗。应对：阶段 2 逐 token 过 WCAG 对比度，正文 ≥4.5:1，辅助文字 ≥3:1 |
| R3 | **云端同步环境依赖** | 中 | 资料库同步只在 workbuddy 页面环境有效；整合页若独立部署（GitHub Pages/Cloudflare），同步自动降级为纯本地。应对：保留现有降级逻辑；「我的」里的同步状态如实显示「本地模式」 |
| R4 | **视频加载失败/耗流量** | 中 | CloudFront 视频国内直连大概率失败。应对：已有纯黑兜底；可加 poster 帧；移动端考虑 `preload="metadata"` 减负 |
| R5 | **双单位系统思维混乱** | 低 | Hero 用 u/h、工作台用 rem，后续维护容易混。应对：CSS 文件分区注释；Hero 区变量加 `hero-` 前缀隔离 |
| R6 | **z-index 层级战争** | 低 | Hero plate(0) / 内容(2) / 菜单(5) / 顶栏(6) vs 工作台 Tab 吸顶 / 弹窗 / Toast。应对：合并时定义全站层级表：视频 0 < 内容 2 < 吸顶 Tab 10 < 菜单 20 < 弹窗 30 < Toast 40 < 彩带 50 |
| R7 | **logoipsum 位置语义** | 低 | 规格里 logo 条在 Hero 底部 fade 里；移到页尾后 Hero 底部会空。应对：阶段 1 验收时确认底部 fade 渐变在无 logo 时仍成立（成立，渐变本就是为收边） |
| R8 | **功能冲突：两个"状态"** | 低 | Hero 入场动画（元素带 animation both）与工作台 IO reveal 若作用于同区域会打架。应对：分区管理——Hero 元素的入场动画类与工作台 .rv 类不混用 |

---

## 六、不做的事（明确边界）

- 不重写业务逻辑（连续天数/ETA/休息日规则已验证，原样搬）
- 不把 Hero 规格参数做任何"优化"（像素锁定是红线）
- 不引入框架（保持单文件、无构建步骤的交付形态）
- 不改数据结构（localStorage key、云端表结构不变，老用户数据无损）

---

## 附：里程碑速查

| 里程碑 | 产出 | 验收标准 |
|--------|------|---------|
| M1（阶段 1 完） | 可滚动的落地页 | Hero 像素回归通过 |
| M2（阶段 3 完） | 功能完整的工作台（临时样式） | 5 模块功能全通 |
| M3（阶段 4 完） | 暗色视觉统一 | 无割裂感、对比度达标 |
| M4（阶段 6 完） | 整合完成 | 全量验收清单通过 |
